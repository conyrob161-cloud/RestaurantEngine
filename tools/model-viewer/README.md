# Model Viewer

Open `tools/model-viewer/index.html` to inspect GLB/GLTF models, animations, skeletons, and basic diagnostics.

Use `?model=../assets/your-model.glb` or the file picker to load a model.
